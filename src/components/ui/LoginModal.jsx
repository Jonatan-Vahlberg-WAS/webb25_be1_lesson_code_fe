import Modal from "./Modal";

export default function LoginModal({ open, onClose }) {
  //TODO: add login form

  return (
    <Modal open={open} onClose={onClose} title="Log in to Motify">
      
    </Modal>
  );
}
